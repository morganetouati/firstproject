<nav class="navbar navbar-default navbar-fixed-top">
    <div id="navbar" class="collapse navbar-collapse pull-right">
        <ul class="nav navbar-nav">
            {% if app.request.get('_route') not in ['homepage', 'entries'] %}
                <li><a href="{{ path("homepage") }}">Home</a></li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN')%}
                <li><a href="{{ path("list_author.twig") }}">List articles</a></li>
                <li><a href="{{ path("admin_create_entry") }}">create article</a></li>
                <li><a href="{{ path("author_create") }}">create author</a></li>
                <li><a href="{{ path("admin_index") }}">Admin</a></li>
                <li><a href="{{ path('logout') }}">logout </a></li>
            {% elseif is_granted('ROLE_USER') %}
                <li><a href="{{ path('logout') }}">logout </a></li>
            {% else %}
                <li><a href="{{ path('login') }}">Login</a></li>
                <li><a href="{{ path('register') }}">Registration</a></li>
            {% endif %}
        </ul>
    </div>
</nav>
