<nav class="navbar navbar-expand-md navbar-inverse bg-faded">
    <div class="collapse navbar-collapse" id="nav-content">
        <ul class="nav navbar-nav">
            {% if app.request.get('_route') not in ['homepage', 'entries'] %}
                <li><a href="{{ path("homepage") }}">Home</a></li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
                <li class="dropdown">
                    <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button"
                       aria-haspopup="true"
                       aria-expanded="false">Article</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path("admin_create_entry") }}">Create article</a>
                        <a class="dropdown-item" href="{{ path("admin_entries") }}">List articles</a>
                    </div>
                </li>

                <li class="dropdown">
                    <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button"
                       aria-haspopup="true"
                       aria-expanded="false">Author</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path("list_author") }}">List author</a>
                        <a class="dropdown-item" href="{{ path("author_create") }}">Create author</a>
                    </div>
                </li>
            {% endif %}
            {% if is_granted('ROLE_USER') %}
                <li><a href="{{ path('logout') }}">Logout</a></li>
            {% else %}

                <li><a href="{{ path('login') }}">Login</a></li>
                <li><a href="{{ path('register') }}">Registration</a></li>
            {% endif %}
        </ul>
    </div>
</nav>